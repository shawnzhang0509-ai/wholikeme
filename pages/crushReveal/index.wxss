.result { margin-top: 48rpx; animation: rise .7s ease both; }
.note {
  margin-top: 28rpx;
  color: var(--muted);
  font-size: 22rpx;
  line-height: 1.65;
}
@keyframes rise { from { opacity: 0; transform: translateY(20rpx); } to { opacity: 1; transform: none; } }
