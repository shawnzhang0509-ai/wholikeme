.title { animation: rise .8s ease both; }
.subtitle { animation: rise .8s .12s ease both; }
ui-button { margin-bottom: 24rpx; }
@keyframes rise { from { opacity: 0; transform: translateY(24rpx); } to { opacity: 1; transform: none; } }
